import styles from "./page.module.scss";

const ParentProfile = () => {
  return (
    <main className={styles.main}>
      <div>Parent Profile Here</div>
    </main>
  );
};

export default ParentProfile;
